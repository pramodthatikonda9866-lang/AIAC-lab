"""number = int(input("Enter a number: "))

if number >= 0:
	digits = str(number)
	power = len(digits)
	total = sum(int(digit) ** power for digit in digits)

	if total == number:
		print("Armstrong number")
	else:
		print("Not an Armstrong number")
else:
	print("Not an Armstrong number")"""
 
 #generate a code where it should check whether the given input is a armstronge number or not. If it is a armstrong number, the output should be "armstrong number"; else, "Not an Armstrong number".
